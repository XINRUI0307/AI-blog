---
phase: 02-discovery
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - models.py
  - routes/albums.py
  - templates/albums/list.html
  - templates/albums/detail.html
  - templates/albums/create.html
  - templates/posts/detail.html
  - templates/base.html
  - app.py
autonomous: true
requirements: [DISC-05, DISC-06, DISC-07, DISC-08, DISC-09]

must_haves:
  truths:
    - "Any authenticated user can create an album with a name and optional description"
    - "Album creator can add any post to their album"
    - "Album creator can remove posts from their album"
    - "Non-owner cannot add/remove posts from someone else's album"
    - "Any visitor can view an album's posts without logging in"
    - "Any visitor can browse a list of all albums without logging in"
  artifacts:
    - path: "models.py"
      provides: "Album model and album_posts association table"
      contains: "class Album(db.Model)"
    - path: "routes/albums.py"
      provides: "albums_bp blueprint with CRUD and browse routes"
      contains: "albums_bp = Blueprint"
    - path: "templates/albums/list.html"
      provides: "Album list page"
      contains: "albums"
    - path: "templates/albums/detail.html"
      provides: "Album detail page showing posts"
      contains: "album.posts"
    - path: "templates/albums/create.html"
      provides: "Album creation form"
      contains: 'name="name"'
  key_links:
    - from: "routes/albums.py"
      to: "models.py"
      via: "Album and Post model imports"
      pattern: "from models import Album, Post"
    - from: "routes/albums.py"
      to: "models.py"
      via: "album.user_id ownership check"
      pattern: "album\\.user_id != current_user\\.id"
    - from: "templates/posts/detail.html"
      to: "routes/albums.py"
      via: "Add to Album form"
      pattern: "url_for\\('albums\\.add_post'"
    - from: "app.py"
      to: "routes/albums.py"
      via: "Blueprint registration"
      pattern: "from routes\\.albums import albums_bp"
    - from: "templates/base.html"
      to: "routes/albums.py"
      via: "Albums nav link"
      pattern: "url_for\\('albums\\.list_albums'"
---

<objective>
Add albums: Album model with ownership, CRUD routes, album browse pages, "Add to Album" on post detail, and nav link.

Purpose: Any authenticated user can create curated collections of posts; anyone can browse albums (DISC-05 through DISC-09).
Output: Album model, album_posts table, migration, album routes, album templates, nav link.
</objective>

<execution_context>
@C:/Users/igf19/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/igf19/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-discovery/02-RESEARCH.md
@.planning/phases/02-discovery/02-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From models.py (after Plan 01 — Tag model already added):
```python
from extensions import db
from datetime import datetime

class Post(db.Model):
    __tablename__ = 'posts'
    id = db.Column(db.Integer, primary_key=True)
    title = db.Column(db.String(200), nullable=False)
    author_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=False)
    # ... tags relationship added by Plan 01

class User(UserMixin, db.Model):
    __tablename__ = 'users'
    id = db.Column(db.Integer, primary_key=True)
    username = db.Column(db.String(80), unique=True, nullable=False)
    role = db.Column(db.String(20), nullable=True)
```

From extensions.py:
```python
db = SQLAlchemy()
```

From app.py (blueprint registration pattern — tags_bp added by Plan 01):
```python
for bp in (auth_bp, posts_bp, photos_bp, comments_bp,
           membership_bp, profile_bp, search_bp, admin_bp, tags_bp):
    app.register_blueprint(bp)
```

From utils.py:
```python
def role_required(*roles): ...
def contributor_required(f): ...
```

From templates/base.html (navbar structure for adding Albums link):
```html
<ul class="navbar-nav me-auto">
  <li class="nav-item">
    <a class="nav-link" href="{{ url_for('posts.index') }}">
      <i class="bi bi-house me-1"></i>Home
    </a>
  </li>
  <li class="nav-item">
    <a class="nav-link" href="{{ url_for('search.results') }}">
      <i class="bi bi-search me-1"></i>Search
    </a>
  </li>
</ul>
```

From templates/posts/detail.html (owner actions area for "Add to Album" button):
```html
{% if current_user.is_authenticated and (current_user.id == post.author_id or current_user.role == 'admin') %}
<div class="d-flex flex-wrap gap-2 mb-4">
  <a href="{{ url_for('posts.edit', post_id=post.id) }}" ...>Edit</a>
  ...
</div>
{% endif %}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Album model, migration, routes blueprint, and app wiring</name>
  <files>models.py, routes/albums.py, app.py</files>
  <read_first>models.py, app.py, extensions.py, routes/posts.py</read_first>
  <action>
1. **models.py** -- Add `album_posts` secondary table and `Album` model AFTER the existing `Tag` model (added by Plan 01). Do NOT use `added_at` on the secondary table (per research pitfall 2 -- `db.Table` does not invoke Python-side defaults):

   ```python
   album_posts = db.Table(
       'album_posts',
       db.Column('album_id', db.Integer, db.ForeignKey('albums.id'), primary_key=True),
       db.Column('post_id', db.Integer, db.ForeignKey('posts.id'), primary_key=True),
   )

   class Album(db.Model):
       __tablename__ = 'albums'
       id = db.Column(db.Integer, primary_key=True)
       user_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=False)
       name = db.Column(db.String(200), nullable=False)
       description = db.Column(db.Text, default='')
       created_at = db.Column(db.DateTime, default=datetime.utcnow)

       owner = db.relationship('User', backref='albums')
       posts = db.relationship('Post', secondary=album_posts, backref='albums')

       def __repr__(self):
           return f'<Album {self.name}>'
   ```

2. **routes/albums.py** -- Create NEW file with all album routes. Key routes:

   ```python
   from flask import Blueprint, render_template, redirect, url_for, flash, request, abort
   from flask_login import login_required, current_user
   from extensions import db
   from models import Album, Post

   albums_bp = Blueprint('albums', __name__)

   @albums_bp.route('/albums')
   def list_albums():
       page = request.args.get('page', 1, type=int)
       albums = Album.query.order_by(Album.created_at.desc()).paginate(page=page, per_page=12)
       return render_template('albums/list.html', albums=albums)

   @albums_bp.route('/album/<int:album_id>')
   def detail(album_id):
       album = Album.query.get_or_404(album_id)
       return render_template('albums/detail.html', album=album)

   @albums_bp.route('/album/create', methods=['GET', 'POST'])
   @login_required
   def create():
       if request.method == 'POST':
           name = request.form['name'].strip()
           if not name:
               flash('Album name is required.', 'danger')
               return render_template('albums/create.html')
           album = Album(
               user_id=current_user.id,
               name=name,
               description=request.form.get('description', '').strip()
           )
           db.session.add(album)
           db.session.commit()
           flash('Album created.', 'success')
           return redirect(url_for('albums.detail', album_id=album.id))
       return render_template('albums/create.html')

   @albums_bp.route('/album/<int:album_id>/add/<int:post_id>', methods=['POST'])
   @login_required
   def add_post(album_id, post_id):
       album = Album.query.get_or_404(album_id)
       if album.user_id != current_user.id and current_user.role != 'admin':
           abort(403)
       post = Post.query.get_or_404(post_id)
       if post not in album.posts:
           album.posts.append(post)
           db.session.commit()
           flash('Post added to album.', 'success')
       else:
           flash('Post is already in this album.', 'info')
       return redirect(request.referrer or url_for('albums.detail', album_id=album_id))

   @albums_bp.route('/album/<int:album_id>/remove/<int:post_id>', methods=['POST'])
   @login_required
   def remove_post(album_id, post_id):
       album = Album.query.get_or_404(album_id)
       if album.user_id != current_user.id and current_user.role != 'admin':
           abort(403)
       post = Post.query.get_or_404(post_id)
       if post in album.posts:
           album.posts.remove(post)
           db.session.commit()
           flash('Post removed from album.', 'success')
       return redirect(request.referrer or url_for('albums.detail', album_id=album_id))
   ```

   Note: `list_albums` is paginated (12 per page) per DISC-09. `create` uses `@login_required` only (no `contributor_required`) so readers can also create albums per DISC-05. `add_post` and `remove_post` check `album.user_id != current_user.id` for ownership per research recommendation.

3. **app.py** -- Register `albums_bp`:
   - Add import: `from routes.albums import albums_bp`
   - Add `albums_bp` to the blueprint tuple in the `for bp in (...)` loop.

4. **Run migration:**
   ```bash
   flask db migrate -m "add albums and album_posts tables"
   flask db upgrade
   ```
  </action>
  <verify>
    <automated>cd C:/Users/igf19/blog-ai && python -c "from models import Album, album_posts; assert hasattr(Album, 'posts'); assert hasattr(Album, 'owner'); print('Album model OK')" && python -c "from routes.albums import albums_bp; print('Albums blueprint OK')" && flask db current 2>&1 | head -3</automated>
  </verify>
  <acceptance_criteria>
    - models.py contains `class Album(db.Model)` with `__tablename__ = 'albums'`
    - models.py contains `album_posts = db.Table('album_posts'`
    - models.py Album has `user_id = db.Column(db.Integer, db.ForeignKey('users.id'), nullable=False)`
    - models.py Album has `owner = db.relationship('User', backref='albums')`
    - models.py Album has `posts = db.relationship('Post', secondary=album_posts, backref='albums')`
    - album_posts table does NOT contain `added_at` column
    - routes/albums.py contains `albums_bp = Blueprint('albums', __name__)`
    - routes/albums.py contains `def list_albums():` with `.paginate(page=page, per_page=12)`
    - routes/albums.py contains `def detail(album_id):`
    - routes/albums.py contains `def create():` with `@login_required` (NOT `@contributor_required`)
    - routes/albums.py contains `def add_post(album_id, post_id):` with `album.user_id != current_user.id` check
    - routes/albums.py contains `def remove_post(album_id, post_id):` with same ownership check
    - app.py contains `from routes.albums import albums_bp`
    - app.py blueprint tuple includes `albums_bp`
    - A migration file exists in migrations/versions/ containing `op.create_table('albums'`
    - `flask db current` shows head revision
  </acceptance_criteria>
  <done>Album and album_posts tables exist in database. Album CRUD routes work: list (paginated), detail (public), create (login required), add_post (owner only), remove_post (owner only). Blueprint registered in app.</done>
</task>

<task type="auto">
  <name>Task 2: Album templates, "Add to Album" on post detail, and nav link</name>
  <files>templates/albums/list.html, templates/albums/detail.html, templates/albums/create.html, templates/posts/detail.html, templates/base.html</files>
  <read_first>templates/posts/index.html, templates/posts/detail.html, templates/base.html, routes/albums.py</read_first>
  <action>
1. **templates/albums/create.html** -- Create NEW directory `templates/albums/` and file. Follow the existing card/form pattern from `templates/posts/create.html`:

   ```html
   {% extends 'base.html' %}
   {% block title %}Create Album — Photo Blog{% endblock %}
   {% block content %}

   <div class="page-header">
     <h2><i class="bi bi-collection me-2"></i>Create Album</h2>
     <p>Create a collection to organize your favorite posts.</p>
   </div>

   <div class="card">
     <div class="card-body" style="padding:2rem">
       <form method="post">
         <div class="mb-4">
           <label class="form-label">Name <span class="text-danger">*</span></label>
           <input type="text" name="name" class="form-control form-control-lg"
                  placeholder="Give your album a name..." required maxlength="200"
                  style="font-family:var(--font-serif);font-size:1.15rem">
         </div>
         <div class="mb-4">
           <label class="form-label">
             Description
             <span class="text-muted fw-normal">(optional)</span>
           </label>
           <textarea name="description" class="form-control" rows="4"
                     placeholder="What is this album about?"></textarea>
         </div>
         <div class="d-flex gap-2">
           <button type="submit" class="btn btn-primary">
             <i class="bi bi-check2 me-1"></i>Create Album
           </button>
           <a href="{{ url_for('albums.list_albums') }}" class="btn btn-outline-secondary">Cancel</a>
         </div>
       </form>
     </div>
   </div>

   {% endblock %}
   ```

2. **templates/albums/detail.html** -- Album detail page showing posts in card grid. Include remove button for album owner:

   ```html
   {% extends 'base.html' %}
   {% block title %}{{ album.name }} — Photo Blog{% endblock %}
   {% block content %}

   <div class="page-header">
     <h2><i class="bi bi-collection me-2"></i>{{ album.name }}</h2>
     {% if album.description %}
     <p>{{ album.description }}</p>
     {% endif %}
     <p class="text-muted small">
       Created by
       <a href="{{ url_for('profile.view', username=album.owner.username) }}">{{ album.owner.username }}</a>
       &middot; {{ album.posts|length }} post{{ 's' if album.posts|length != 1 }}
     </p>
   </div>

   {% if album.posts %}
   <div class="row g-4">
     {% for post in album.posts %}
     <div class="col-sm-6">
       <div class="card post-card">
         {% if post.images %}
         <a href="{{ url_for('posts.detail', post_id=post.id) }}">
           <img src="{{ url_for('photos.serve_post_photo', post_id=post.id, filename=post.images[0].filename) }}"
                class="post-card-img" alt="{{ post.title }}">
         </a>
         {% else %}
         <div class="post-card-img-placeholder">
           <i class="bi bi-image" style="font-size:2rem; opacity:0.4"></i>
         </div>
         {% endif %}
         <div class="card-body">
           <a href="{{ url_for('posts.detail', post_id=post.id) }}" class="post-card-title d-block">
             {{ post.title }}
           </a>
           <div class="post-meta mt-2">
             <span>
               <i class="bi bi-person me-1"></i>
               <a href="{{ url_for('profile.view', username=post.author.username) }}"
                  style="color:inherit; font-weight:500">{{ post.author.username }}</a>
             </span>
             <span class="post-meta-dot">&middot;</span>
             <span><i class="bi bi-calendar3 me-1"></i>{{ post.created_at.strftime('%d %b %Y') }}</span>
           </div>
           <p class="post-excerpt">{{ post.description }}</p>
           <div class="d-flex gap-2 align-items-center">
             <a href="{{ url_for('posts.detail', post_id=post.id) }}"
                class="btn btn-outline-primary btn-sm">
               Read more <i class="bi bi-arrow-right ms-1"></i>
             </a>
             {% if current_user.is_authenticated and (current_user.id == album.user_id or current_user.role == 'admin') %}
             <form method="post" action="{{ url_for('albums.remove_post', album_id=album.id, post_id=post.id) }}"
                   class="d-inline" onsubmit="return confirm('Remove this post from the album?')">
               <button type="submit" class="btn btn-outline-danger btn-sm">
                 <i class="bi bi-x-circle me-1"></i>Remove
               </button>
             </form>
             {% endif %}
           </div>
         </div>
       </div>
     </div>
     {% endfor %}
   </div>
   {% else %}
   <p class="text-muted fst-italic">This album has no posts yet.</p>
   {% endif %}

   {% endblock %}
   ```

3. **templates/albums/list.html** -- Paginated list of all albums:

   ```html
   {% extends 'base.html' %}
   {% block title %}Albums — Photo Blog{% endblock %}
   {% block content %}

   <div class="page-header">
     <h2><i class="bi bi-collection me-2"></i>Albums</h2>
     <p>Browse curated photo collections.</p>
   </div>

   {% if current_user.is_authenticated %}
   <div class="mb-4">
     <a href="{{ url_for('albums.create') }}" class="btn btn-primary btn-sm">
       <i class="bi bi-plus-circle me-1"></i>Create Album
     </a>
   </div>
   {% endif %}

   {% if albums.items %}
   <div class="row g-4">
     {% for album in albums.items %}
     <div class="col-sm-6">
       <div class="card">
         <div class="card-body">
           <h5 class="card-title">
             <a href="{{ url_for('albums.detail', album_id=album.id) }}" style="color:inherit; text-decoration:none;">
               <i class="bi bi-collection me-1"></i>{{ album.name }}
             </a>
           </h5>
           {% if album.description %}
           <p class="text-muted small mb-2">{{ album.description[:150] }}{% if album.description|length > 150 %}...{% endif %}</p>
           {% endif %}
           <div class="post-meta">
             <span>
               <i class="bi bi-person me-1"></i>
               <a href="{{ url_for('profile.view', username=album.owner.username) }}"
                  style="color:inherit; font-weight:500">{{ album.owner.username }}</a>
             </span>
             <span class="post-meta-dot">&middot;</span>
             <span><i class="bi bi-images me-1"></i>{{ album.posts|length }} post{{ 's' if album.posts|length != 1 }}</span>
             <span class="post-meta-dot">&middot;</span>
             <span><i class="bi bi-calendar3 me-1"></i>{{ album.created_at.strftime('%d %b %Y') }}</span>
           </div>
         </div>
       </div>
     </div>
     {% endfor %}
   </div>

   {% if albums.has_prev or albums.has_next %}
   <nav class="mt-4">
     <ul class="pagination justify-content-center">
       {% if albums.has_prev %}
       <li class="page-item">
         <a class="page-link" href="{{ url_for('albums.list_albums', page=albums.prev_num) }}">
           <i class="bi bi-chevron-left"></i> Previous
         </a>
       </li>
       {% endif %}
       <li class="page-item disabled">
         <span class="page-link">Page {{ albums.page }} of {{ albums.pages }}</span>
       </li>
       {% if albums.has_next %}
       <li class="page-item">
         <a class="page-link" href="{{ url_for('albums.list_albums', page=albums.next_num) }}">
           Next <i class="bi bi-chevron-right"></i>
         </a>
       </li>
       {% endif %}
     </ul>
   </nav>
   {% endif %}

   {% else %}
   <p class="text-muted fst-italic">No albums yet. Be the first to create one!</p>
   {% endif %}

   {% endblock %}
   ```

4. **templates/posts/detail.html** -- Add "Add to Album" dropdown AFTER the tags display block (added by Plan 01) and BEFORE the owner actions block. This is visible to any authenticated user who owns at least one album:

   ```html
   {% if current_user.is_authenticated and current_user.albums %}
   <div class="mb-3">
     <div class="dropdown d-inline-block">
       <button class="btn btn-outline-secondary btn-sm dropdown-toggle" type="button"
               data-bs-toggle="dropdown" aria-expanded="false">
         <i class="bi bi-collection me-1"></i>Add to Album
       </button>
       <ul class="dropdown-menu">
         {% for album in current_user.albums %}
         <li>
           <form method="post" action="{{ url_for('albums.add_post', album_id=album.id, post_id=post.id) }}">
             <button type="submit" class="dropdown-item">{{ album.name }}</button>
           </form>
         </li>
         {% endfor %}
       </ul>
     </div>
   </div>
   {% endif %}
   ```

5. **templates/base.html** -- Add "Albums" link in the left-side navbar, AFTER the Search link:

   ```html
   <li class="nav-item">
     <a class="nav-link" href="{{ url_for('albums.list_albums') }}">
       <i class="bi bi-collection me-1"></i>Albums
     </a>
   </li>
   ```
  </action>
  <verify>
    <automated>cd C:/Users/igf19/blog-ai && python -c "
import os
# Check all album templates exist
for f in ['templates/albums/create.html', 'templates/albums/detail.html', 'templates/albums/list.html']:
    assert os.path.exists(f), f'{f} missing'
# Check create.html
c = open('templates/albums/create.html').read()
assert 'name=\"name\"' in c, 'create missing name input'
assert 'name=\"description\"' in c, 'create missing description input'
# Check detail.html
d = open('templates/albums/detail.html').read()
assert 'album.posts' in d, 'detail missing posts iteration'
assert 'albums.remove_post' in d, 'detail missing remove button'
# Check list.html
l = open('templates/albums/list.html').read()
assert 'albums.items' in l, 'list missing pagination items'
assert 'albums.has_next' in l, 'list missing pagination'
# Check post detail has add-to-album
pd = open('templates/posts/detail.html').read()
assert 'albums.add_post' in pd, 'post detail missing add-to-album'
# Check base.html has albums link
b = open('templates/base.html').read()
assert 'albums.list_albums' in b, 'base missing albums nav link'
print('All album template checks passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - templates/albums/create.html exists, extends base.html, has `name="name"` required input and `name="description"` textarea
    - templates/albums/detail.html exists, iterates `album.posts`, shows album name/description/owner, has remove button for owner
    - templates/albums/detail.html remove button is wrapped in `current_user.id == album.user_id` check
    - templates/albums/list.html exists, iterates `albums.items`, has pagination with `albums.has_prev`/`albums.has_next`
    - templates/albums/list.html has "Create Album" button visible to authenticated users
    - templates/posts/detail.html contains `url_for('albums.add_post'` in a dropdown form
    - templates/posts/detail.html "Add to Album" dropdown is inside `current_user.albums` check
    - templates/base.html contains `url_for('albums.list_albums')` nav link with `bi-collection` icon
  </acceptance_criteria>
  <done>Album creation form, album detail page with remove buttons, paginated album list, "Add to Album" dropdown on post detail, and "Albums" link in navigation bar are all in place.</done>
</task>

</tasks>

<verification>
Manual browser verification (no automated test suite per project decisions):

1. Start app: `flask run`
2. Visit `/albums` without logging in -- verify public list page loads (empty)
3. Log in as any user (reader or contributor)
4. Click "Albums" in nav bar -- verify link works
5. Click "Create Album" -- fill in name "Favorites" and description "My best picks", submit
6. Verify redirected to album detail page showing "Favorites" with 0 posts
7. Visit any post detail page -- verify "Add to Album" dropdown appears with "Favorites"
8. Click "Favorites" in the dropdown -- verify post added, flash message shown
9. Visit `/album/<id>` -- verify post appears in album
10. Click "Remove" on the post in album detail -- verify post removed
11. Log in as a DIFFERENT user -- visit the same album -- verify no remove button visible
12. Log out -- visit `/albums` and `/album/<id>` -- verify both load without login
</verification>

<success_criteria>
- Album and album_posts tables exist in the database (migration applied)
- Any authenticated user (reader or contributor) can create an album
- Album creator can add any post to their album via dropdown on post detail
- Album creator can remove posts from their album
- Non-owner sees no add/remove controls
- Album list page at `/albums` is public and paginated
- Album detail page at `/album/<id>` is public and shows all posts
- "Albums" link appears in navigation bar
</success_criteria>

<output>
After completion, create `.planning/phases/02-discovery/02-02-SUMMARY.md`
</output>
